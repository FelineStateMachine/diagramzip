{
  "name": "diagramzip-shell",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:assets": "npm --prefix .. run build:diagramzip",
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit",
    "deploy": "npm run build:assets && wrangler deploy",
    "deploy:dry": "npm run build:assets && wrangler deploy --dry-run",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260821.1",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.125.0"
  }
}
