# Incomplete input still renders: the squares that fit are drawn, the rest of
# the rectangle is hatched, and the caption explains where placement stopped.
# A "?" square is as wide as the gap it lands in.
.view both
title Sketch in progress

rectangle 33 x 32
(18 15) (7 8) (14 ?)
