# Diagram examples

`diagrams/` is the canonical cross-runtime source corpus for DiagramZip. The
editor imports a curated subset for its homepage examples, while renderer smoke
checks and unit tests reuse the same inputs.

Keep renderer-specific regression examples here when they are valid diagram
sources. Put malformed or intentionally invalid inputs beside the test that owns
them instead of adding them to this corpus.
