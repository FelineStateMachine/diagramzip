[project]
name = "diagramzip-symbolator"
version = "0.1.0"
description = "Direct Python Worker Symbolator-compatible HDL component renderer"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "workers-py",
  "workers-runtime-sdk",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["test"]
